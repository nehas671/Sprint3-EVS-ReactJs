import React from 'react'
import { useSelector } from 'react-redux'

/*const ViewDistrict = (props)=>{

    let listDistrict = useSelector(state=> district);

    const dispatch = useDispatch();
    const myInput = useRef();
    React.useEffect(() => {
        DistrictList()
      }, []);

      const DistrictList = () => {
        dispatch(viewScheduleAction())
      }
}*/